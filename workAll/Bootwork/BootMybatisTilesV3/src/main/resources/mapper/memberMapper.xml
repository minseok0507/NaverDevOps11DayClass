<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.example.demo.data.mapper.MemberMapperInter">
	<!-- <select id="getTotalCount" resultType="int">
		select count(*) from memberdb
	</select> -->

<!--	<select id="searchIdCheck" parameterType="String" resultType="int">-->
<!--		select count(*)-->
<!--		from memberdb-->
<!--		where myid = #{myid}-->
<!--	</select>-->

<!--	<insert id="insertMember" parameterType="com.example.demo.data.dto.MemberDto">-->
<!--        insert into memberdb (name, myid, passwd, addr, hp, email, photo, birthday, registday)-->
<!--        values (#{name}, #{myid}, #{passwd}, #{addr}, #{hp}, #{email}, #{photo}, #{birthday}, now())-->
<!--    </insert>-->
</mapper>